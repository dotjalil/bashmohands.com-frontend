export default function LoginPage() {
  return (
    <>
      <h1>Login Page</h1>
      {/* TODO
            1. Add login form: email address, password
            2. Add signup link: redirect to /signup
            3. Submit user's data via the action below.
        */}
    </>
  );
}

export async function action({ request }) {
  // TODO...
}
